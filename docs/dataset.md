# Dataset Support

Planned adapters support UMN Unusual Crowd Activity, GBA-Stampedes, GSMADC, and additional legally usable crowd datasets.

Each adapter should expose a dataset path, class labels, and configurable train/validation/test proportions. The initial planned split is 70% training, 15% validation, and 15% testing. No dataset metrics are displayed until a real run produces them.