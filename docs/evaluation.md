# Evaluation

Evaluation is pending until a labeled dataset and reproducible evaluation run are configured.

The planned report includes accuracy, precision, recall, F1-score, false-alarm rate, response time, processing FPS, and density MAE/RMSE where ground-truth counts exist. The product intentionally renders `Evaluation Pending` rather than inventing metrics.